%table.table.table-striped
  %caption Earnings
  %thead
    %tr
      %th
      %th Date
      %th.numeric Amount
  %tbody
    - @earnings_by_day.each do |record|
      %tr
        %td= isk_image
        %td= record['date']
        %td.numeric= number_to_isk record['amount']
= paginate @earnings_by_day
