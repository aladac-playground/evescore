%ol.breadcrumb
  %li.breadcrumb-item
    = link_to 'Factions', factions_path
  %li.breadcrumb-item.active Factions-less Groups

.panel.panel-default
  .panel-body
    - @groups.each do |group|
      %p= link_to group.name, factionless_group_path(group_id: group.id)
